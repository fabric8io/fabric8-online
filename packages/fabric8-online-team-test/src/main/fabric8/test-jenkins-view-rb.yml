 metadata:
   name: jenkins-view
   namespace: ${PROJECT_NAMESPACE}-test
 roleRef:
   name: view
 subjects:
 - kind: ServiceAccount
   name: jenkins
 userNames:
 - system:serviceaccount:${PROJECT_NAMESPACE}-jenkins:jenkins